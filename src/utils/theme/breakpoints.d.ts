import {
  Breakpoints,
  BreakpointsOptions,
} from "@/utils/theme/types/breakpoints.types";

export declare namespace createBreakpoints {
  export function createBreakpoints(
    breakpoints: BreakpointsOptions
  ): Breakpoints;
}
